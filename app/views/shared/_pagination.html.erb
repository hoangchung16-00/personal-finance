<nav aria-label="Page navigation example">
  <ul class="pagination">
    <li class="page-item <%= "disabled" if pagy.prev.nil? %>">
      <%= link_to(resources_path(controller_name, page: pagy.prev), class: "page-link", aria: { label: 'Previous' }) do %>
        <span aria-hidden="true">&laquo;</span>
        <span class="sr-only">Previous</span>
      <% end %>
    </li>
    <% (1..pagy.pages).each do |page| %>
      <li class="page-item <%= "disabled" if pagy.page == page %>">
        <%= link_to page, resources_path(controller_name, page:), class: "page-link" %>
      </li>
    <% end %>
    <li class="page-item <%= "disabled" if pagy.next.nil? %>">
      <%= link_to(resources_path(controller_name, page: pagy.next), class: "page-link", aria: { label: 'Next' }) do %>
        <span aria-hidden="true">&raquo;</span>
        <span class="sr-only">Next</span>
      <% end %>
    </li>
  </ul>
</nav>
